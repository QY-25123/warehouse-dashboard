# Local environment files — never commit these
.env.local
.env.*.local
.env

# Google OAuth credentials — contain secrets, never commit
gsheet_credentials.json
gmail_credentials.json
client_secret.json
client_secret_*.json
get_gsheet_token.py
get_gmail_token.py

# Dependencies
node_modules/

# Next.js build output
frontend/.next/
frontend/out/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# deepeval local telemetry/cache — created by running the eval suite
.deepeval/
.deepeval_telemetry.txt

# macOS
.DS_Store
